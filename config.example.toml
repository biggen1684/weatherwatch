# Run with -zip to find your zone code and add it below
zone = "FLZ112"
# Add your two letter state abbreviation below
area = "FL"
# Valid event strings come from the NWS API's "event" field
events = ["Tornado Warning", "Severe Thunderstorm Warning", "Heat Advisory"]
